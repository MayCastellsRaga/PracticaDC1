# HANDS ON Mongo
### Start the Mongo docker

```bash
docker compose up -d
```

#### SET UP the influxDB
Navigate to the "localhost:7474" and create the collection test

#### We can load data in multiple ways (see documentation online)
load the movies test
### We can go to the UI to experiment with the visualization
play with the movies test tutorial
