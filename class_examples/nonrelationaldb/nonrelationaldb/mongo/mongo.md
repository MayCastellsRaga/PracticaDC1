# HANDS ON Mongo
### Start the Mongo docker

```bash
docker compose up -d
```

#### SET UP the influxDB
Navigate to the "localhost:8081" and create the collection test

#### We can load data in multiple ways (see documentation online)
We can load the `records.json` directly using the web application

### We can go to the UI to experiment with the visualization
